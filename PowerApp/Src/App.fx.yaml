App As appinfo:
    BackEnabled: =true
    Formulas: =
    OnStart: |-
        =Set(pouplista,false);
        Set(pouplista_2,false);
        Set(pouplista_3,false);
        Set(pouplista_4,false);
        Set(displayModeVar,DisplayMode.View);
        Set(varEstadoKr,"sin aprobación");
        Set(lastID, form_Registrar.LastSubmit.nombreKR);
        Set(usuarioActual,User().Email);
        Set(vistaDetalle,true)
    Theme: =pacifico1

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

